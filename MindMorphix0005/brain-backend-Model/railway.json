{
  "build": {
    "builder": "nixpacks",
    "config": {
      "baseImage": "python:3.9",
      "installCommand": "pip install -r requirements.txt",
      "startCommand": "gunicorn main:app"
    }
  }
}
